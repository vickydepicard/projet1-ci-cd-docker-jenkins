version: "3.9"

services:
  web:
    build: .
    ports:
      - "9003:80"
    volumes:
      - ../../src/:/usr/share/nginx/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    environment:
      - ENV=development
    env_file:
      - cat ../../env/.env.dev

